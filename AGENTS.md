Read `.ai/PROJECT_CONTEXT.md` before making any changes.
It contains the full project structure, technical rules, data flow, and common task patterns.
Always follow the rules in that file — especially: no fallback WPP, no global sort ratio fallback, weight-based NG Rate only.
